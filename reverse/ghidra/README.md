# Ghidra Helpers

`DroneWin32Labels.py` recreates stable Phase 1 Win32 labels in a local Ghidra project. It intentionally contains only labels that are useful enough to persist; low-confidence hypotheses remain in the ledgers/prose until stronger evidence is available.

See `docs/reverse/GHIDRA_WORKFLOW.md` for import, identity, naming, and export rules.
